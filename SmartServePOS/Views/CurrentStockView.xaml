<Page
	x:Class="SmartServePOS.Views.CurrentStockView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:hc="https://handyorg.github.io/handycontrol"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:viewmodels="clr-namespace:SmartServePOS.ViewModels"
	d:DataContext="{d:DesignInstance Type=viewmodels:CurrentStockViewModel}"
	Background="#F5F5F5"
	mc:Ignorable="d">

	<DockPanel Margin="12">

		<!--  ================= HEADER =================  -->
		<Grid Margin="0,0,0,12" DockPanel.Dock="Top">
			<Grid.ColumnDefinitions>
				<ColumnDefinition />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<TextBlock
				VerticalAlignment="Center"
				FontSize="20"
				FontWeight="SemiBold"
				Text="Current Stock" />

			<StackPanel
				Grid.Column="1"
				VerticalAlignment="Center"
				Orientation="Horizontal">

				<TextBox
					Width="200"
					Height="30"
					Margin="0,0,8,0"
					VerticalContentAlignment="Center"
					Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

				<Button
					Width="90"
					Height="30"
					Command="{Binding RefreshCommand}"
					Content="Refresh" />
			</StackPanel>
		</Grid>

		<!--  ================= CONTENT =================  -->
		<Border
			Padding="8"
			Background="White"
			CornerRadius="6">

			<Grid>
				<!--  Loader  -->
				<!--<hc:Loading IsLoading="{Binding IsLoading}" />-->

				<!--  Stock Grid  -->
				<DataGrid
					Margin="0"
					AutoGenerateColumns="False"
					CanUserAddRows="False"
					GridLinesVisibility="None"
					HeadersVisibility="Column"
					IsReadOnly="True"
					ItemsSource="{Binding Stocks}"
					RowHeight="36">

					<!--  Row Style (Low Stock Highlight)  -->
					<DataGrid.RowStyle>
						<Style TargetType="DataGridRow">
							<Style.Triggers>
								<DataTrigger Binding="{Binding IsLowStock}" Value="True">
									<Setter Property="Background" Value="#FFFDECEA" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</DataGrid.RowStyle>

					<DataGrid.Columns>

						<DataGridTextColumn
							Width="*"
							Binding="{Binding ItemName}"
							Header="Item" />

						<DataGridTextColumn
							Width="120"
							Binding="{Binding ItemType}"
							Header="Type" />

						<DataGridTextColumn
							Width="100"
							Binding="{Binding CurrentQuantity}"
							Header="Stock" />

						<DataGridTextColumn
							Width="80"
							Binding="{Binding Unit}"
							Header="Unit" />

						<DataGridTextColumn
							Width="100"
							Binding="{Binding MinStockLevel}"
							Header="Min Level" />

						<!--  Status Column  -->
						<DataGridTemplateColumn Width="120" Header="Status">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<TextBlock
										HorizontalAlignment="Center"
										FontWeight="SemiBold"
										Foreground="#C62828"
										Text="LOW"
										Visibility="{Binding IsLowStock, Converter={StaticResource BoolToVisibilityConverter}}" />
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>

					</DataGrid.Columns>
				</DataGrid>

			</Grid>
		</Border>

	</DockPanel>
</Page>
